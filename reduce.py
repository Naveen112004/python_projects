# from functools import reduce
# numbers = [1, 2, 3, 4, 5]
# result = reduce(lambda x, y: x + y, numbers)
# print(result)

# from functools import reduce
#
# numbers = [1, 2, 3, 4]
#
# result = reduce(lambda x, y: x * y, numbers)
#
# print(result)
#
# from functools import reduce
#
# numbers = [10, 50, 20, 90, 30]
#
# result = reduce(lambda x, y: x if x > y else y, numbers)
#
# print(result)

# from functools import reduce
#
# numbers = [10, 50, 20, 90, 30]
#
# result = reduce(lambda x, y: x if x < y else y, numbers)
#
# print(result)

# from functools import reduce
#
# words = ["Python", " ", "is", " ", "awesome"]
#
# result = reduce(lambda x, y: x + y, words)
#
# print(result)

# from functools import reduce
# words = ["cat", "elephant", "dog", "python"]
# result = reduce(lambda x, y: x if len(x) > len(y) else y,words)
# print(result)
#
# from functools import reduce
#
# n = 5
#
# result = reduce(lambda x, y: x * y, range(1, n + 1))
#
# print(result)
#
# from functools import reduce
#
# number = "12345"
#
# result = reduce(lambda x, y: x + int(y),number,0)
#
# print(result)
#
# from functools import reduce
#
# words = ["Python", "Java", "C"]
#
# result = reduce(lambda total, word: total + len(word),words,0)
# print(result)